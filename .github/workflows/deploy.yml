name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Quartz dependencies
        run: |
          cd node_modules/@jackyzha0/quartz
          npm ci

      - name: Setup Quartz content
        run: |
          # Create content directory in Quartz
          mkdir -p node_modules/@jackyzha0/quartz/content
          
          # Copy content from obsidian to Quartz content directory
          cp -r obsidian/* node_modules/@jackyzha0/quartz/content/
          
          # Verify content was copied
          echo "Content directory structure:"
          ls -la node_modules/@jackyzha0/quartz/content/

      - name: Setup custom styles
        run: |
          # Create necessary directories
          mkdir -p node_modules/@jackyzha0/quartz/styles
          
          # Check if we have custom styles to copy
          if [ -d "quartz/styles" ]; then
            echo "Copying custom styles..."
            # Copy all styles and preserve directory structure
            find quartz/styles -type f -name '*.scss' -o -name '*.css' | while read -r file; do
              # Create target directory if it doesn't exist
              target_dir="node_modules/@jackyzha0/quartz/$(dirname "${file#quartz/}")"
              mkdir -p "$target_dir"
              # Copy the file
              cp -v "$file" "$target_dir/"
            done
            
            # Verify the files were copied
            echo "Custom styles copied. Directory structure:"
            find node_modules/@jackyzha0/quartz/styles -type f | sort
          else
            echo "Warning: No custom styles directory found at quartz/styles/"
            # Create a basic custom.scss if it doesn't exist
            mkdir -p node_modules/@jackyzha0/quartz/styles
            if [ ! -f "node_modules/@jackyzha0/quartz/styles/custom.scss" ]; then
              echo "// Custom styles will be included here" > node_modules/@jackyzha0/quartz/styles/custom.scss
            fi
          fi
          
          # Verify the styles directory exists and has content
          echo "Final styles directory structure:"
          find node_modules/@jackyzha0/quartz/styles -type f | sort || echo "No styles found"

      - name: Update configuration
        run: |
          # Use GitHub Pages URL format
          BASE_URL="${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}"
          echo "Using base URL: $BASE_URL"
          
          # Update baseUrl in quartz.config.ts
          sed -i "s|baseUrl:.*|baseUrl: \"$BASE_URL\"|g" quartz.config.ts
          
          # Copy config to Quartz
          cp quartz.config.ts node_modules/@jackyzha0/quartz/
          
          # Verify config
          echo "Updated configuration:"
          grep "baseUrl" node_modules/@jackyzha0/quartz/quartz.config.ts

      - name: Build site
        run: |
          cd node_modules/@jackyzha0/quartz
          
          # Build the site
          echo "Building site..."
          npx quartz build --verbose
          
          # Verify build output
          echo "Build output:"
          ls -la public/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: node_modules/@jackyzha0/quartz/public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
